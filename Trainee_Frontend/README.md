# Trainee_Frontend

Simple static frontend. Open `index.html` in a browser, or serve with a static server:

```bash
# from Trainee_Frontend
python3 -m http.server 8080
# then open http://localhost:8080
```

Click "Call Backend" to call the sample backend at `http://localhost:3000/api/hello`.